# Class mod manifest extract

Generated by `scripts/extract_classmod_manifest.mjs` from `Nexus-Data-inv4.json`.

- **category_id** = Root `serialindex.index` on the `classmod_*` inv row (same family id used in `{category_id:partIndex}` codes).
- **rarity** options use real **serial indices** from `comp_01_common` … `comp_05_legendary` sub-compositions.
- **Other slots** (`class_mod_body`, `stat_group*`, …) list part **names** from `parttypeselectionrules`; option `index` values are **positional placeholders** unless you merge **parts_stats_data** / NCS idRaw.

## Next step

Merge these entries into `bl4_manifest.js` `items` array (or load this JSON in a build step). Add matching rows to `ncs_slot_map.js` if you need NCS slot counts for the legit builder.

Regenerate:

```bash
node scripts/extract_classmod_manifest.mjs
```
